networks:
  audio_text_bridge:
    driver: bridge
    ipam:
      config:
        - subnet: 169.254.9.0/24

services:
  app:
    build:
      context: ./
      dockerfile: ./Dockerfile
    networks:
      audio_text_bridge:
        ipv4_address: 169.254.9.2
        aliases:
          - "audio-text-backend.app"
    ports:
      - 3203:3203
    volumes:
      - ./:/app/
      - /app/audio_text_backend.egg-info/
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgres:17.1-alpine
    networks:
      audio_text_bridge:
        ipv4_address: 169.254.9.3
        aliases:
          - "audio-text-backend.db"
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./pgsql/init.d:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    networks:
      audio_text_bridge:
        ipv4_address: 169.254.9.4
        aliases:
          - "audio-text-backend.cache"
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  celery-worker:
    build: ./
    networks:
      audio_text_bridge:
        ipv4_address: 169.254.9.5
        aliases:
          - "audio-text-backend.celery"
    volumes:
      - ./:/app/
    environment:
      - CELERY_BROKER_URL=redis://audio-text-backend.cache:6379/0
      - CELERY_RESULT_BACKEND=redis://audio-text-backend.cache:6379/0
      - C_FORCE_ROOT="true"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: celery -A audio_text_backend.celery.app worker --loglevel=info -Q audio_processing

  # TODO not working yet, connection to redis issue
  # flower:
  #   image: mher/flower:2.0
  #   networks:
  #     audio_text_bridge:
  #       ipv4_address: 169.254.9.6
  #       aliases:
  #         - "audio-text-backend.flower"
  #   ports:
  #     - "5555:5555"
  #   environment:
  #     - CELERY_BROKER_URL=redis://audio-text-backend.cache:6379/0
  #     - FLOWER_PORT=5555
  #     - C_FORCE_ROOT="true"
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  #     celery-worker:
  #       condition: service_started
  #   command: celery flower --broker=redis://audio-text-backend.cache:6379/0 --port=5555

volumes:
  redis_data:
  postgres_data:
